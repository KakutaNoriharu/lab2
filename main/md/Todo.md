# 20260510

## 攻撃者特定：前提の整理と方向性

### 攻撃モデルの前提
- 攻撃ノードはまず正規ノードから情報(0)を受信し、その後 FAKE(1) の配信を開始する（経路の根ではない）
- 攻撃ノードは受信した経路履歴の全 ID を 1 に書き換えて再送信する
- 正規ノードは 0 と 1 のどちらが偽情報かを識別できない
- 正規ノードは受信した経路履歴の長さを比較し、長い方を信頼する設計

### 齟齬（矛盾）の構造
攻撃者 M が B から情報(0)を受信し、全 ID を 1 に書き換えて再送信すると：

```
「0」の経路: S(0) → A(0) → B(0) → D(0)
「1」の経路: S(1) → A(1) → B(1) → M(1) → D(1)
                ↑ 捏造（本来は 0 を送った）    ↑ ここが攻撃者
```

- B は「0」チェーンでは 0 を送ったのに「1」チェーンでは 1 を送ったことになる → 矛盾
- **M は「1」チェーンにしか現れない** → これが攻撃者特定の手がかり

### 攻撃者特定の論理
```
各ノード X に対して：
  「0」チェーンのいずれかの経路履歴に X が出現する → A=true
  「1」チェーンのいずれかの経路履歴に X が出現する → B=true

  B=true かつ A=false → 攻撃者候補（「1」チェーンにしか現れない）
  B=true かつ A=true  → 正規ノード（両チェーンにまたがる共通中継者）
```

### 既存の可視化との対応
- `AnalyzeAccPlot.ipynb` の `content_0` / `content_1` 集計がこの矛盾を可視化している
- `r.csv`（Source, Target, Content の受信ログ）が元データ
- **疑惑スコアの自動計算・定量評価はまだ未実装**（可視化のみ）

### 今後の方向性

#### オフライン検出（シミュレーション後分析）
- `AnalyzeAccPlot.ipynb` に疑惑スコア計算セルを追加
- 「0」チェーン出現ノード集合と「1」チェーン出現ノード集合の差集合を計算
- `malicious_broadcast_counter` を正解ラベルとして検出率・誤検出率を定量評価
- 実装コスト：低

#### オンライン検出（シミュレーション中にリアルタイム判断）
- 各ノードが受信した経路履歴を蓄積し、矛盾スコアをリアルタイム更新
- 疑惑スコアが閾値を超えたノードからの情報をブラックリスト化
- 前提：ノード間で経路履歴を共有する仕組みが必要（method 9 / 999 の完成が必要）
- 実装コスト：高

### 実装状況
- 攻撃者特定ロジック（共通ノード検出・ブラックリスト・疑惑スコア）は**完全に未実装**
  - `method3.hpp`, `simulation.hpp`, `store.hpp`, `node.hpp` いずれにも該当コードなし

## 未実装（空セルで終わっている）

  - 自動的な攻撃者判定ロジック — content_1 > content_0
  の赤表示で視覚的には区別できるが、「このノードが攻撃者」と判定するスコアリングや閾値処理はない
  - 根本発信元の遡及 — from チェーンを辿ってFAKE情報の源泉ノードを特定するロジックがない
  - 精度評価 — 正解ラベル（実際の MALICIOUS ノード）との照合がない
  - 最後の2セルが空のまま

### 現状のまとめ

伝播ツリーの可視化までは動いており、FakeをたくさんSendしたノードを赤で目視確認できる状態です。ただし「どのノードが攻撃者か」を自動的に出力・評価する部分は未実装です。

# 20260408
* Gitのbrunchなどの機能理解

* シミュレーションの複数回実行の自動化
    - シェルスクリプトでやるか、コマンドライン引数でやるか
    - パラメータファイルをcsvで用意して、`-p`オプションでコマンドライン引数として受け取り、総当たり実行してくれる
    - 実際のフォーマットはparameterLoader.hppに依存する

* シミュレーションの実行速度の高速化
    - 提案手法のアルゴリズムの見直し
    - 二分探索木の導入など
    - 処理が遅い箇所の特定

## 長時間・高密度環境における経路履歴の肥大化への対策
提案手法では各ノードが受信した情報に付随する経路履歴（転送ノードのIDと情報内容の組）を保持・比較することで真の情報を選択する．しかし，シミュレーション期間が長くなるほど，また TA 内のノード密度が高くなるほど，情報が多数のノードを経由し経路履歴の長さが際限なく増加する問題が生じる．経路履歴が肥大化すると，各ノードの記憶領域の消費が増大し，情報の送受信時の通信コストも上昇するため，実用的なシステムとしての動作が困難となる可能性がある．\
この問題への対策として，経路履歴に有効期限（TTL: Time To Live）を設け，一定ホップ数または一定時間を超えた古い履歴エントリを自動的に削除する手法が考えられる．あるいは，経路履歴の全エントリを保持するのではなく，経由ノード数のカウントのみを保持する軽量化手法も有効な選択肢である．ただし，単純なカウントのみでは攻撃者が虚偽のカウント値を付与して偽情報を優勢化させる恐れがあるため，カウントの改ざん耐性を確保する仕組みも合わせて検討する必要がある．今後の研究では，履歴の圧縮・削減がもたらす真情報保持率への影響をシミュレーションにより定量評価し，許容できる性能低下の範囲内で最も効率的な履歴管理方式を明らかにする．

## 経路履歴情報を用いた攻撃ノードの特定
現在の提案手法は，経路履歴長の比較によって偽情報を「排除」することを目的としており，偽情報を発信している攻撃ノードそのものを「特定・隔離」する機能は持っていない．しかし，攻撃ノードを特定できれば，そのノードからの情報を以降一切受け付けないブラックリスト方式により，よりロバストな防御が実現できる．\
経路履歴には各転送ノードのIDが記録されているため，複数のノードが受信した偽情報の経路履歴を照合することで，偽情報の発生源となっているノードを特定できる可能性がある．具体的には，異なる経路を経由して届いた複数の偽情報の履歴を比較し，すべての偽情報の経路に共通して出現するノードIDを攻撃ノードの候補とみなす手法が考えられる．ただし，攻撃ノードが自身の ID を偽装したり，複数の ID を使い分けるなりすまし攻撃（Sybil 攻撃）を行う場合には検出が困難となる．そのため，ノード ID の真正性を保証する軽量な認証方式との組み合わせも今後の検討課題として挙げられる．これらの手法をシミュレーション上に実装し，攻撃ノード特定の精度（検出率・誤検出率）および特定までの所要時間を評価することで，実用化に向けた知見を得ることを目指す．